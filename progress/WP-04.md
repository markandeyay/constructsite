# WP-04: Copy

Owns exactly one file: `src/content/copy.ts`.

- CLAIMED 2026-09-18T01:20:00Z
- DONE 2026-09-18T01:34:00Z

## What shipped

`src/content/copy.ts` exports `copy` (`as const`) in the exact shape frozen in
BUILD_CONTRACT C3, plus `GITHUB_URL`. Source text is spec section 11,
transcribed faithfully except where BUILD_CONTRACT section A overrides it.

## Overrides applied (contract wins over spec 11)

| Override | Applied |
|---|---|
| A1 | No `company` key. No banned brand token anywhere, including comments. Site title and wordmark unchanged. |
| A1 | Hero kicker is `DESIGN AUTOMATION FOR BIOLOGY`. |
| C3 | H1 split into `hero.h1a` / `hero.h1b`. |
| Q3 | Problem section is prose only. No `stats` key, no numbers invented. |
| Q4 / App. B | Traction item 2 uses the DATA SOURCE wording verbatim from Appendix B. Heading is the neutral "Grounded in real data". The word "partnered" appears nowhere. |
| Q9 | "the corpus our retrieval runs against". Never "trained on". |
| Q12 | Structure caption no longer links the structure to the demo construct. It describes GFP on its own terms. |
| A2 | No `team` key, no section 08 team copy, no personal names. |
| A3 | `contact.num` is `08`. |
| A4 | `contact.error` is exactly `Something went wrong. Please try again in a moment.` No email address anywhere in the file. |
| A4 / 11.10 | `footer.left` = `Construct. Chapel Hill, NC.`, `footer.right` = `GitHub · 2026`. No mailto, no address. |
| Q10 | No `biosec` key at all. |
| 11.11 | Header ghost label is `View the code`. |
| Q6 | `GITHUB_URL = 'https://github.com/markandeyay/constructsite'`. |

Note: `contact.options` retains `Partnership` because that is a contact-form
subject option from spec 10.2 (what the sender wants to discuss), not a claim
that any partnership exists. It is a different word from the banned "partnered"
and it makes no assertion about a third party.

## APPENDIX B WALK, row by row

Every row of the Appendix B table, walked against every string actually written.

| Appendix B row | Strings in this file that touch it | Verdict |
|---|---|---|
| **Addgene relationship** | `traction.items[1].b` = "Built on Addgene's plasmid repository, the field's most comprehensive dataset, which is the corpus our retrieval runs against." `traction.items[1].h` = "Grounded in real data". | COMPLIANT. The body is the Appendix B allowed default wording, character for character. The heading asserts no relationship of any kind. "Backed by", "investor", "licensed", "partnered" and any logo reference are absent. No Addgene mark is named for display. |
| **Validation accuracy** | `validation.goldset` = "36 known-good · 52 known-bad · 100% combined accuracy on the curated set"; `traction.items[3].b` = "100% combined accuracy across a curated gold set of known-good and known-bad constructs." | COMPLIANT. Both are spec section 11 wording. Neither says "100% accurate", "never wrong", or "perfect accuracy". Both carry "curated" within 60 characters of the "100%" (measured, see lint output). The counts 36 / 52 are the spec's own sourced gold-set figures, not invented. |
| **Commit count** | none | COMPLIANT. No commit number, no repo-volume metric, anywhere in the file. |
| **Open source** | `hero.status` = "LIVE · OPEN SOURCE · MIT LICENSED"; `traction.items[2].b` = "MIT licensed and public on GitHub, so the engine can be read, not just described." | COMPLIANT. The license claim is the MIT/public-repo claim Appendix B allows. Nothing calls the company open source or community-built. The hero status line is verbatim spec 11.1. |
| **Determinism** | `validation.title` = "Checked by rules, not guesses."; `validation.body2` ("a deterministic validation engine ... follows fixed rules, so the same design always produces the same verdict"); `how.steps[3].line` ("A rules engine checks it against biology and manufacturing constraints."). | COMPLIANT. Uses both allowed phrasings. The determinism is scoped to the engine's behavior, never to the biology. No "guaranteed correct", no "provably valid", no claim that the underlying science is settled. `validation.body3` explicitly says validation is not a pass/fail stamp, which cuts against over-reading it. |
| **Wet lab** | `structure.body` ("They get synthesized, put into cells, and folded into proteins"); `problem.body2` ("weeks of lab time"). | COMPLIANT. Both describe the industry's workflow in general, not Construct's operations. Nothing says "validated in the lab", "our lab", or "our results". No first-person wet-lab activity is claimed anywhere. |
| **Location** | `footer.left` = "Construct. Chapel Hill, NC." | COMPLIANT. Place only. No lab space, office, facility, or headquarters claim. |
| **Product status** | `traction.items[0]` = "Live today" / "The full pipeline runs end to end. Describe, retrieve, generate, validate, export."; `hero.status` "LIVE". | COMPLIANT. Uses the allowed "live and working today" / "the full pipeline runs end to end" framing. No user count, no lab count, no "in production at N". The lint's invented-user-count regex returns clean. |
| **Output breadth** | `outputs.cards[*]` including "Therapeutic proteins and cell therapy", "CAR-T constructs", "Viral vectors (AAV, lentivirus)", "mRNA constructs"; `outputs.closing`. | COMPLIANT. Every card names a design category as a platform capability. No clinical status, no "FDA", no "approved therapy", no implication that any output has been used in a patient. Card 3's "modern therapeutics" describes the category of construct, not Construct's role in any therapy. |
| **Time savings** | `hero.lede` = "Work that takes an expert days now takes minutes."; `outputs.cards[0].body` "designed and checked in minutes". | COMPLIANT. The exact qualitative sentence Appendix B allows. No "Nx faster", no multiplier, no dollar figure. |
| **Team** | none | COMPLIANT. Contract A2 deletes the team section. No names, no roles, no advisors, no affiliations, no personal links. |
| **Funding** | none | COMPLIANT. Nothing about a raise, valuation, investor, or runway. The contact form's `Investment` subject option is an inbound routing label from spec 10.2 and asserts nothing about funding received. |
| **B.1 biosecurity line** | none | COMPLIANT with Q10. There is no `biosec` key. No screening claim is made anywhere in the file. |

Additional unsourced-claim sweep beyond the table: the only numerals in the
file are the section numerals (02..08), the gold-set counts 36 / 52 / 100%
(spec 11.5 and 11.7), the PDB entry 1EMA (spec 9.3 and 11.11), and the footer
year 2026. No other number appears. The problem section stat row was deleted
per Q3 rather than filled with estimates.

## LINT OUTPUT (spec 16.5 regexes, run against the finished file)

Throwaway runner: the ten section 16.5 regexes verbatim, plus the 60-character
"curated" proximity rule, plus contract A1/A4 banned-token checks.

```
ok    em dash (16.5)
ok    en dash used as a connector (16.5)
ok    emoji (2.4)
ok    unsourced speed multiplier (Appendix B)
ok    overstated Addgene relationship (Appendix B)
ok    unverified Addgene claim (Appendix B)
ok    accuracy claim missing its qualifier (Appendix B)
ok    clinical claim (Appendix B)
ok    unverified deployment claim (Appendix B)
ok    invented user count (Appendix B)
ok    banned brand token PMR (contract A1)
ok    banned brand token (contract A1)
ok    email address (contract A4)
ok    partnership claim (Appendix B / Q4)
ok    "trained on" (Q9)
ok    "100%" #1 qualifier within 60 chars: "100%\" sits within 60 characters of the word \"curated\".\n *  -"
ok    "100%" #2 qualifier within 60 chars: "100% combined accuracy on the curated set',\n    rows: [\n    "
ok    "100%" #3 qualifier within 60 chars: "100% combined accuracy across a curated gold set of known-go"

"100%" occurrences: 3, unqualified: 0
COPY LINT: PASS (0 violations)
```

("100%" #1 is the rule restated in the file's own header comment; it satisfies
the proximity rule as written.)

WP-16 owns the permanent lint. This run was not blocked on it.

## Typecheck

`npx tsc --noEmit` at repo root: clean, no output. `strict: true`,
`noUnusedLocals`, `noUnusedParameters` all on.

## Notes for consumers

- `copy.contact.success` carries the literal token `{email}` for interpolation
  by WP-12.
- `copy.hero.h1a` / `h1b` are the two headline lines; WP-09a renders both inside
  the single `<h1>`.
- There is no `copy.team`, no `copy.problem.stats`, no `biosec`, and no
  `company` key. Do not add them.
- Middle dot separators are U+00B7 (`·`), not a dash. Safe under the 16.5 rules.

## Two-line summary

Wrote `src/content/copy.ts`: every user-facing string, typed `as const`, in the
frozen C3 shape, with all BUILD_CONTRACT overrides applied. Appendix B walked
row by row and the section 16.5 lint run clean, both recorded above.
